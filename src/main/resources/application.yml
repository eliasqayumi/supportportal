jwt:
  secret: secret-1%32Turkiye!An!@$ISTas2#%SDT
server:
  port: 5000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supportHome
    username: root
    password: Turk@emlak58
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB
  mvc:
    throw-exception-if-no-handler-found: true
    resources:
      add-mappings: false
